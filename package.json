{
  "name": "maayo",
  "version": "0.0.0",
  "private": true,
  "description": "Protocol-first, backend-agnostic offline sync library",
  "scripts": {
    "build": "nx run-many -t build",
    "test": "nx run-many -t test",
    "lint": "nx run-many -t lint",
    "build:protocol": "nx build @maayo/protocol",
    "build:client": "nx build @maayo/client",
    "typecheck": "nx run-many -t typecheck"
  },
  "devDependencies": {
    "@nx/js": "^22.7.5",
    "@nx/workspace": "^22.7.5",
    "nx": "^22.7.5",
    "typescript": "~5.8.3",
    "vitest": "^4.0.9",
    "@vitest/coverage-v8": "^4.0.9",
    "tslib": "^2.8.1",
    "prettier": "^3.5.3"
  }
}
